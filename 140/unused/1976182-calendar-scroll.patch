# HG changeset patch
# User Betterbird <betterbird@betterbird.eu>
# Date 1760604900 -7200
# Parent  e7df72a832a43f3584e202e32c95969db423f710
Bug 1976182 - Fix calendar scrolling issue with horrible hack for now.

diff --git a/calendar/base/content/calendar-multiday-view.js b/calendar/base/content/calendar-multiday-view.js
--- a/calendar/base/content/calendar-multiday-view.js
+++ b/calendar/base/content/calendar-multiday-view.js
@@ -3473,8 +3473,10 @@
       const pos = Math.round(Math.max(0, minute) * this.pixelsPerMinute);
       if (this.getAttribute("orient") == "horizontal") {
         this.grid.scrollLeft = document.dir == "rtl" ? -pos : pos;
       } else {
+        // Horrible hack to fix scrolling problem for now.
+        this.grid.scrollTop = 0;
         this.grid.scrollTop = pos;
       }
       // NOTE: this.scrollMinute is set by the "scroll" callback.
       // This means that if we tried to scroll further than possible, the
