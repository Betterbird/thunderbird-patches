
# HG changeset patch
# User welpy-cw <h.w.forms@arcor.de>
# Date 1756314366 14400
# Node ID 2c1f49de6b6663526f9f90ccece1ee135cf046ea
# Parent  d44a825b43462a713be9ce8b101258057a9b046e
Bug 1667865 - Hide the "Reply" button in header view for newsgroup messages. r=mkmelin

Differential Revision: https://phabricator.services.mozilla.com/D262556

diff --git a/mail/base/content/msgHdrView.js b/mail/base/content/msgHdrView.js
--- a/mail/base/content/msgHdrView.js
+++ b/mail/base/content/msgHdrView.js
@@ -3538,17 +3538,18 @@ function UpdateReplyButtons() {
     followupButton.hidden = buttonToShow != "followup";
   }
 
   const replyToSenderButton = document.getElementById("hdrReplyToSenderButton");
   if (replyToSenderButton) {
     if (FeedUtils.isFeedMessage(gMessage)) {
       replyToSenderButton.hidden = true;
     } else if (smartReplyButton) {
-      replyToSenderButton.hidden = buttonToShow == "reply";
+      replyToSenderButton.hidden =
+        buttonToShow == "reply" || buttonToShow == "followup";
     } else {
       replyToSenderButton.hidden = false;
     }
   }
 }
 
 /**
  * Update the enabled/disabled states of the Reply, Reply-All, Reply-List,

