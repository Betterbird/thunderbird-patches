# HG changeset patch
# User welpy-cw <h.w.forms@arcor.de>
# Date 1772414505 -39600
# Parent  f9a722d04271c8d0f7ec73d4b5283db5f05d6ba4
Bug 2019941 - Correct bitwise logic in nsMsgDatabase::ClearNewList to properly clear New flag from database.

diff --git a/mailnews/db/msgdb/src/nsMsgDatabase.cpp b/mailnews/db/msgdb/src/nsMsgDatabase.cpp
--- a/mailnews/db/msgdb/src/nsMsgDatabase.cpp
+++ b/mailnews/db/msgdb/src/nsMsgDatabase.cpp
@@ -2720,13 +2720,13 @@ NS_IMETHODIMP nsMsgDatabase::ClearNewLis
       if (msgHdr) {
         uint32_t flags;
         (void)msgHdr->GetFlags(&flags);
 
-        if ((flags | nsMsgMessageFlags::New) != flags) {
+        if (flags & nsMsgMessageFlags::New) {
           msgHdr->AndFlags(~nsMsgMessageFlags::New, &flags);
-          NotifyHdrChangeAll(msgHdr, flags | nsMsgMessageFlags::New, flags,
-                             nullptr);
         }
+        NotifyHdrChangeAll(msgHdr, flags | nsMsgMessageFlags::New, flags,
+                           nullptr);
       }
       if (elementIndex == 0) {
         break;
       }
