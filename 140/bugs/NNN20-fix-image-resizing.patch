# HG changeset patch
# User alta88@fixall.com, Betterbird <betterbird@betterbird.eu> and DeepSeek
# Date 1784808672 -7200
# Parent  f1fa704c060f005a8ea1b393e441e5d3db13ea7e
NNN20 - Fix image resizing regressions.

diff --git a/mail/app/profile/all-thunderbird.js b/mail/app/profile/all-thunderbird.js
--- a/mail/app/profile/all-thunderbird.js
+++ b/mail/app/profile/all-thunderbird.js
@@ -1566,4 +1566,7 @@ pref("mail.threadpane.cardsview.rowcount
 // Fit font rendering.
 pref("gfx.font_rendering.cleartype_params.force_gdi_classic_max_size", 15);
 pref("gfx.font_rendering.cleartype_params.force_gdi_classic_for_families",
   "Arial,Consolas,Courier New,Microsoft Sans Serif,Segoe UI,Tahoma,Trebuchet MS,Verdana");
+
+// Image zooming in links.
+pref("mail.allow_zooming_image_links", false);
diff --git a/mail/base/content/aboutMessage.js b/mail/base/content/aboutMessage.js
--- a/mail/base/content/aboutMessage.js
+++ b/mail/base/content/aboutMessage.js
@@ -66,44 +66,55 @@ function getMessagePaneBrowser() {
 }
 
 /**
  * Handle "resize" events on the messagepane.
+ * Re-evaluates [overflowing] on all images in the zoom system
+ * (selected by img:is([shrinktofit],[overflowing])).
+ * If an image is in full-size mode ([overflowing] only, no [shrinktofit])
+ * and the window is now wide enough, snaps it back to constrained mode.
  */
 async function messagePaneOnResize() {
   const doc = getMessagePaneBrowser().contentDocument;
   // Bail out if it's http content or we don't have images.
   if (doc?.URL.startsWith("http") || !doc?.images) {
     return;
   }
 
-  const availableWidth = Math.max(
-    document.body.scrollWidth,
-    window.visualViewport.width
+  // Note that visualViewport.width is a float, and this property returns 0
+  // for chrome windows (in case someone naïly tests that).
+  const availableWidth = Math.floor(
+    Math.max(document.body.scrollWidth, window.visualViewport.width)
   );
 
-  const adjustImg = img => {
+  const adjustImg = (img, allowZoom) => {
     if (img.hasAttribute("shrinktofit")) {
-      // overflowing: Whether the image is overflowing visible area.
-      img.toggleAttribute("overflowing", img.naturalWidth > img.clientWidth);
+      const isOverflowing = img.naturalWidth > img.clientWidth;
+      img.toggleAttribute("overflowing", isOverflowing && allowZoom);
     } else if (img.hasAttribute("overflowing")) {
       const isOverflowing = img.clientWidth >= availableWidth;
-      img.toggleAttribute("overflowing", isOverflowing);
-      img.toggleAttribute("shrinktofit", !isOverflowing);
+      img.toggleAttribute("overflowing", isOverflowing && allowZoom);
+      img.toggleAttribute("shrinktofit", !(isOverflowing && allowZoom));
     }
   };
 
+  const zoomImageLinks = Services.prefs.getBoolPref(
+    "mail.allow_zooming_image_links",
+    false
+  );
   for (const img of doc.querySelectorAll(
     "img:is([shrinktofit],[overflowing])"
   )) {
-    if (img.closest("[href]")) {
-      continue;
-    }
+    const isLink = !!img.closest("[href]");
     if (!img.complete) {
-      img.addEventListener("load", event => adjustImg(event.target), {
-        once: true,
-      });
+      img.addEventListener(
+        "load",
+        event => adjustImg(event.target, !isLink || zoomImageLinks),
+        {
+          once: true,
+        }
+      );
     } else {
-      adjustImg(img);
+      adjustImg(img, !isLink || zoomImageLinks);
     }
   }
 }
 
diff --git a/mail/base/content/msgHdrView.js b/mail/base/content/msgHdrView.js
--- a/mail/base/content/msgHdrView.js
+++ b/mail/base/content/msgHdrView.js
@@ -1096,25 +1096,58 @@ var messageProgressListener = {
         });
       });
     }
 
-    // Scale any overflowing images, exclude http content.
+    // Initialize the image zoom system. Images get [shrinktofit] if their
+    // width is greater than minimum viewport width to constrain them with
+    // correct aspect ratio, and [overflowing] if they exceed the viewport width
+    // AND zooming is allowed (non-links, or links with the pref set).
+    // The CSS in messageBody.css defines three visual states based on
+    // these two attributes. Click toggling happens in contentAreaClick.js
+    // and resize handling in aboutMessage.js (messagePaneOnResize).
     if (!browser.contentDocument.URL.startsWith("http")) {
-      const adjustImg = img => {
-        img.toggleAttribute("overflowing", img.naturalWidth > img.clientWidth);
+      // Note that visualViewport.width is a float, and this property returns 0
+      // for chrome windows (in case someone naïly tests that).
+      const availableWidth = Math.floor(
+        Math.max(document.body.scrollWidth, window.visualViewport.width)
+      );
+      const adjustImg = (img, allowZoom) => {
+        // Do not adjust images smaller than the messagepane viewport width.
+        const shrinktofit = img.width > 200;
+        let isOverflowing =
+          img.clientWidth > availableWidth ||
+          img.naturalWidth > img.clientWidth;
+        // Adding shrinktofit attribute will cause a CSS constraint which
+        // alters the overflowing calculation and may create a false overflow.
+        // If the image overflows both before and after the constraint, then it
+        // is zoomable.
+        img.toggleAttribute("shrinktofit", shrinktofit);
+        isOverflowing &&= img.naturalWidth > img.clientWidth;
+        img.toggleAttribute(
+          "overflowing",
+          shrinktofit && isOverflowing && allowZoom
+        );
       };
+      const zoomImageLinks = Services.prefs.getBoolPref(
+        "mail.allow_zooming_image_links",
+        false
+      );
       for (const img of browser.contentDocument.images) {
-        // No zooming for children of clickable links.
-        if (img.closest("[href]")) {
-          continue;
-        }
-        img.toggleAttribute("shrinktofit", true);
+        img.classList.add("moz-message-image");
+        const isLink = !!img.closest("[href]");
+        // Any overflowing image must be click zoomable. An overflowing link
+        // image will have the url displayed on hover and can be opened via
+        // contextmenu.
         if (!img.complete) {
-          img.addEventListener("load", event => adjustImg(event.target), {
-            once: true,
-          });
+          img.addEventListener(
+            "load",
+            event => adjustImg(event.target, !isLink || zoomImageLinks),
+            {
+              once: true,
+            }
+          );
         } else {
-          adjustImg(img);
+          adjustImg(img, !isLink || zoomImageLinks);
         }
       }
     }
   },
diff --git a/mail/themes/shared/mail/messageBody.css b/mail/themes/shared/mail/messageBody.css
--- a/mail/themes/shared/mail/messageBody.css
+++ b/mail/themes/shared/mail/messageBody.css
@@ -118,24 +118,37 @@ mailattachcount {
 @media print {
   * { page: auto !important; }
 }
 
-/* ::::: images ::::: */
-
-img {
+/* ::::: image zoom system ::::: */
+/*
+ * Two attributes (shrinktofit, overflowing) create three visual states
+ * that the JS in msgHdrView.js, aboutMessage.js and contentAreaClick.js
+ * toggles between:
+ *
+ *  [shrinktofit]                    Image constrained to viewport width.
+ *  [shrinktofit][overflowing]       Same, plus zoom-in cursor. Click to expand.
+ *  [overflowing]:not([shrinktofit]) Image at full size, zoom-out cursor.
+ *                                   Click to shrink back.
+ *
+ * Non-linked images start in state 2 if overflowing, or state 1 otherwise.
+ * Linked images default to state 1 only (overflowing never set) unless
+ * pref mail.allow_zooming_image_links is true.
+ * Clicking an image with [overflowing] toggles its [shrinktofit] attribute,
+ * cycling between states 2 and 3. Window resize re-evaluates overflow.
+ */
+img.moz-message-image {
   &[shrinktofit] {
     max-inline-size: 100% !important;
+    block-size: auto !important;
 
     &[overflowing] {
-      cursor: zoom-in;
-      /* If we detect we're shrinking the image, force it to keep its aspect
-       * ratio, since it possibly had hard-coded sizings. */
-      block-size: auto !important;
+      cursor: zoom-in !important;
     }
   }
 
   &[overflowing]:not([shrinktofit]) {
-    cursor: zoom-out;
+    cursor: zoom-out !important;
     /* Unbound the sizing of the image so it is displayed at full size. */
     block-size: auto !important;
     inline-size: auto !important;
     max-inline-size: none !important;
