# HG changeset patch
# User Betterbird <betterbird@betterbird.eu>
# Date 1773575661 -39600
# Parent  74cc968871c67da81b761035ff6d49037ea0371d
Misc: Include protocol in password prompt.

diff --git a/mailnews/base/src/MailAuthenticator.sys.mjs b/mailnews/base/src/MailAuthenticator.sys.mjs
--- a/mailnews/base/src/MailAuthenticator.sys.mjs
+++ b/mailnews/base/src/MailAuthenticator.sys.mjs
@@ -420,9 +420,9 @@ export class Pop3Authenticator extends I
       params
     );
     const promptTitle = composeBundle.formatStringFromName(
       "pop3EnterPasswordPromptTitleWithUsername",
-      [this._server.hostName]
+      [this._server.hostName + " (POP)"]
     );
     return this._server.wrappedJSObject.getPasswordWithUIAsync(
       promptString,
       promptTitle
@@ -449,9 +449,9 @@ export class ImapAuthenticator extends I
       params
     );
     const promptTitle = composeBundle.formatStringFromName(
       "imapEnterPasswordPromptTitleWithUsername",
-      [this._server.hostName]
+      [this._server.hostName + " (IMAP)"]
     );
     return this._server.wrappedJSObject.getPasswordWithUIAsync(
       promptString,
       promptTitle
diff --git a/mailnews/imap/src/nsImapIncomingServer.cpp b/mailnews/imap/src/nsImapIncomingServer.cpp
--- a/mailnews/imap/src/nsImapIncomingServer.cpp
+++ b/mailnews/imap/src/nsImapIncomingServer.cpp
@@ -1976,9 +1976,11 @@ nsImapIncomingServer::PromptPassword(nsI
   nsresult rv = GetStringBundle();
   NS_ENSURE_SUCCESS(rv, rv);
 
   AutoTArray<nsString, 1> formatStrings;
-  CopyUTF8toUTF16(userName, *formatStrings.AppendElement());
+  // `userName` was used for the format string, which is inconsistent with POP and SMTP.
+  CopyUTF8toUTF16(hostName, *formatStrings.AppendElement());
+  formatStrings[0] += u" (IMAP)"_ns;
 
   nsString passwordTitle;
   rv = m_stringBundle->FormatStringFromName(
       "imapEnterPasswordPromptTitleWithUsername", formatStrings, passwordTitle);
