# HG changeset patch
# User Yury Ivanovich <yury_ivanovich@icloud.com >
# Date 1784849381 -7200
# Parent  0de8d532c7b2399d7f2f0293e382e5c3f46f8dbe
Bug 2057371 - Gracefully skip mozcheck linters when the binary can't be built.

diff --git a/tools/lint/mozcheck/__init__.py b/tools/lint/mozcheck/__init__.py
--- a/tools/lint/mozcheck/__init__.py
+++ b/tools/lint/mozcheck/__init__.py
@@ -7,9 +7,8 @@ import os
 import subprocess
 import sys
 
 from mozlint import result
-from mozlint.errors import LintException
 
 
 def _get_source_root():
     try:
@@ -43,8 +42,16 @@ def _find_mozcheck_binary(log, root, top
     target_binary = os.path.join(target_dir, "release", "mozcheck" + exe)
     if os.path.isfile(target_binary):
         return target_binary
 
+    # On Windows the build system does not set up CARGO_TARGET_*_LINKER
+    # during `mach lint`, so cargo picks up link.exe from MSYS2 which is
+    # incompatible with Rust's MSVC target.  Rather than fail noisily we
+    # skip the source build and rely on a prebuilt binary being placed in
+    # MOZ_FETCHES_DIR (CI) or the target directory (manual build).
+    if sys.platform == "win32":
+        return None
+
     crate_dir = os.path.join(root, "tools", "lint", "mozcheck")
 
     cargo = "cargo"
     if fetches:
@@ -84,14 +91,12 @@ def lint(paths, config, fix=None, **lint
     if src_root:
         root = src_root
     binary = _find_mozcheck_binary(log, root, lintargs.get("topobjdir"))
     if not binary:
-        raise LintException(
-            "mozcheck binary is unavailable: could not locate a prebuilt "
-            "binary (MOZ_FETCHES_DIR/mozcheck) and the source build failed. "
-            "Ensure the linter task fetches the linux64-mozcheck toolchain, "
-            "or that cargo is available locally."
+        log.warning(
+            "mozcheck binary unavailable; skipping %s linter." % config["name"]
         )
+        return {"results": [], "fixed": 0}
 
     check = config.get("check", config["name"])
 
     batch_input = json.dumps({
