# HG changeset patch
# User Betterbird <betterbird@betterbird.eu> and DeepSeek
# Date 1786879236 -7200
# Parent  acdaeb5713db0362cb7fb93b0f771f9a651a2c47
Bug 2063838 - Prevent unnecessary select that obliterates new messages.

diff --git a/mail/base/content/about3Pane.js b/mail/base/content/about3Pane.js
--- a/mail/base/content/about3Pane.js
+++ b/mail/base/content/about3Pane.js
@@ -111,8 +111,9 @@ var multiMessageBrowser;
  */
 var accountCentralBrowser;
 
 var initialDisplayComplete = false;
+var skipSelectOnSameFolder = true;
 
 /**
  * HTML body element handling the general layout of the about3pane.
  */
@@ -134,8 +135,9 @@ var hasDOMContentLoaded = Promise.withRe
  * (such as today, yesterday, etc.) correctly categorized.
  */
 function refreshGroupedBySortView() {
   if (gViewWrapper?.showGroupedBySort) {
+    skipSelectOnSameFolder = false;
     folderTree.dispatchEvent(new CustomEvent("select"));
   }
 }
 
@@ -3057,8 +3059,23 @@ var folderPane = {
     }
   },
 
   async _onSelect() {
+    const uri = folderTree.selectedRow?.uri;
+    // If the selected folder is already displayed, there's nothing to do.
+    // Closing the view here would clear the new-message state of the
+    // displayed folder. Don't bail in multiselection, which is handled below.
+    if (
+      skipSelectOnSameFolder &&
+      uri &&
+      folderTree.selection.size <= 1 &&
+      gViewWrapper?.displayedFolder?.URI == uri
+    ) {
+      console.info("Skipping select on folder", uri);
+      return;
+    }
+    skipSelectOnSameFolder = true;
+
     const isSynthetic = gViewWrapper?.isSynthetic;
     threadPane.saveSelection();
     threadPane.hideIgnoredMessageNotification();
     if (!isSynthetic) {
@@ -3066,9 +3083,8 @@ var folderPane = {
       // already been selected in restoreState().
       messagePane.clearAll();
     }
 
-    const uri = folderTree.selectedRow?.uri;
     if (!uri) {
       gFolder = null;
       return;
     }
@@ -3110,9 +3126,13 @@ var folderPane = {
     document.head.querySelector(`link[rel="icon"]`).href =
       FolderUtils.getFolderIcon(gFolder);
 
     // Clean up any existing view wrapper. This will invalidate the thread tree.
-    gViewWrapper?.close();
+    if (gViewWrapper) {
+      gViewWrapper.close();
+    } else {
+      console.info("No view wrapper to close for", uri);
+    }
 
     if (gFolder.isServer) {
       document.title = gFolder.server.prettyName;
       gViewWrapper = gDBView = threadTree.view = null;
@@ -4413,8 +4433,9 @@ var folderPane = {
     const folder = aFolder;
 
     function editVirtualCallback() {
       if (gFolder == folder) {
+        skipSelectOnSameFolder = false;
         folderTree.dispatchEvent(new CustomEvent("select"));
       }
     }
     window.openDialog(
@@ -5600,8 +5621,9 @@ var threadPane = {
         // Global view flags have changed. Reload the currently selected message
         // list to avoid showing a stale configuration. We could be smart here
         // and check if the currently selected folder is part of the modified
         // folders but forcing a selection is inexpensive and straightforward.
+        skipSelectOnSameFolder = false;
         folderTree.dispatchEvent(new CustomEvent("select"));
         break;
       case "folder-strings-changed":
         threadTree.invalidate();
