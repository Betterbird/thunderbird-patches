
# HG changeset patch
# User ndo84bw <ndo84bw@gmx.de>
# Date 1783615829 0
# Node ID 5682c9c955211b79048d67636faa21f966459df2
# Parent  12fa0b523ac3364969b63ff63f7a783718d1daad
Bug 2051567 - Don't treat write-properties as write access for CardDAV address books. r=mkmelin

Per RFC 3744, write-properties only authorizes PROPPATCH on a resource's
own properties, not creating or modifying address object resources.
Servers grant it on read-only shares, so counting it as a write privilege
wrongly marks a read-only shared address book as writable.

Remove write-properties from writePrivs and add bind, the privilege that
permits creating a new resource via PUT.

Differential Revision: https://phabricator.services.mozilla.com/D309569


diff --git a/mailnews/addrbook/modules/CardDAVUtils.sys.mjs b/mailnews/addrbook/modules/CardDAVUtils.sys.mjs
--- a/mailnews/addrbook/modules/CardDAVUtils.sys.mjs
+++ b/mailnews/addrbook/modules/CardDAVUtils.sys.mjs
@@ -32,18 +32,18 @@ const PRESETS = {
   // Google responds correctly but the provided address returns 404.
   "gmail.com": "https://www.googleapis.com",
   "googlemail.com": "https://www.googleapis.com",
   // For testing purposes.
   "test.invalid": "http://localhost:9999",
 };
 
 // At least one of these ACL privileges must be present to consider an address
-// book writable.
-const writePrivs = ["write", "write-properties", "write-content", "all"];
+// book writable (RFC 3744).
+const writePrivs = ["write", "write-content", "bind", "all"];
 
 // At least one of these ACL privileges must be present to consider an address
 // book readable.
 const readPrivs = ["read", "all"];
 
 export var CardDAVUtils = {
   _contextMap: new Map(),
 

