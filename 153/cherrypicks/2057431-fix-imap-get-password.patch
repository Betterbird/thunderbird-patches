# HG changeset patch
# User Max Emig <mozilla@emigm.ax>
# Parent  02b0dc20bbb1c8f0c147451b196cb9974dce9772
Bug 2057431 - Fix manual IMAP fetching after startup regression.

Differential Revision: https://phabricator.services.mozilla.com/D323503

diff --git a/mailnews/imap/src/nsImapIncomingServer.cpp b/mailnews/imap/src/nsImapIncomingServer.cpp
--- a/mailnews/imap/src/nsImapIncomingServer.cpp
+++ b/mailnews/imap/src/nsImapIncomingServer.cpp
@@ -2018,9 +2018,9 @@ nsImapIncomingServer::AsyncGetPassword(n
     NS_ENSURE_SUCCESS(rv, rv);
   }
 
   MOZ_TRY(mPasswordModule->GetCachedPassword(value));
-  if (value.IsEmpty()) {
+  if (!value.IsEmpty()) {
     aPassword = NS_ConvertUTF8toUTF16(value);
   }
 
   return NS_OK;
