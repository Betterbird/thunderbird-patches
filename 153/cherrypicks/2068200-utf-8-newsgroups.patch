# HG changeset patch
# User Welpy-cw <58345540+Welpy-cw@users.noreply.github.com>
# Date 1790417682 -7200
# Node ID 2cacfccccfa07701b19954fd5af85b2bae305fb4
# Parent  ba6455628e7826a2f3fd47a3c4773d00393fc886
Bug 2068200 - Unescape newsgroup path components before modified UTF-7 conversion. r=edicharry

Bug 1989670 moved URI unescaping after the news-specific modified UTF-7
conversion. Percent-encoded Unicode names therefore bypassed that conversion
and produced Unicode filenames instead of the legacy ASCII filenames.
Besides abandoning existing database paths, this caused canonically
equivalent newsgroup names to share files on normalization-insensitive
filesystems.

Unescape each component before converting news names to modified UTF-7 and
hashing the filename. Keep splitting the escaped path first so encoded
slashes remain part of a folder name rather than becoming path separators.

Add coverage for the four canonically equivalent names from the report,
legacy filename compatibility, ampersand and percent escaping, ordinary
Unicode mail folders, and encoded slashes in mail and news paths.

Differential Revision: https://phabricator.services.mozilla.com/D324006

diff --git a/mailnews/base/src/nsMsgUtils.cpp b/mailnews/base/src/nsMsgUtils.cpp
--- a/mailnews/base/src/nsMsgUtils.cpp
+++ b/mailnews/base/src/nsMsgUtils.cpp
@@ -413,23 +413,24 @@ nsresult NS_MsgCreatePathStringFromFolde
       if (haveFirst) {
         path.AppendLiteral(FOLDER_SUFFIX "/");
       }
 
-      if (aIsNewsFolder) {
-        nsAutoCString tmp;
-        CopyUTF16toMUTF7(pathPiece, tmp);
-        CopyASCIItoUTF16(tmp, pathPiece);
-      }
-
       // To handle safely creating database folders, some paths need to be
       // hashed. Hashing operates on an unescaped string, so we unescape each
       // string individually. We need to do this here because if we were to
       // unescape the URI string prior to separating it into path pieces,
       // characters like `/` would break the path piece parsing.
       nsAutoCString unescapedPathPiece;
       MsgUnescapeString(NS_ConvertUTF16toUTF8(pathPiece), 0,
                         unescapedPathPiece);
-      path += NS_MsgHashIfNecessary(unescapedPathPiece);
+      CopyUTF8toUTF16(unescapedPathPiece, pathPiece);
+
+      if (aIsNewsFolder) {
+        nsAutoCString tmp;
+        CopyUTF16toMUTF7(pathPiece, tmp);
+        CopyASCIItoUTF16(tmp, pathPiece);
+      }
+      path += NS_MsgHashIfNecessary(pathPiece);
       haveFirst = true;
     }
     // look for the next slash
     startSlashPos = endSlashPos + 1;
