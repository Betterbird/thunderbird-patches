
# HG changeset patch
# User Toby Pilling <toby@thunderbird.net>
# Date 1786988404 -3600
# Node ID c6c5f52cc24484a443a1071823cd94a3e254b3d4
# Parent  77e460848f07b91c3eea3d86bf2e4adb0936f04f
Bug 2058447 - Use the application locale for the attendee status summary. r=arschmitz

Differential Revision: https://phabricator.services.mozilla.com/D319189


diff --git a/mail/components/calendar/content/calendar-dialog-attendees-row.mjs b/mail/components/calendar/content/calendar-dialog-attendees-row.mjs
--- a/mail/components/calendar/content/calendar-dialog-attendees-row.mjs
+++ b/mail/components/calendar/content/calendar-dialog-attendees-row.mjs
@@ -150,18 +150,21 @@ class CalendarDialogAttendeesRow extends
         id: "calendar-dialog-attendee-summary-declined",
         args: { count: counts.DECLINED?.length || 0 },
       },
       {
         id: "calendar-dialog-attendee-summary-pending",
         args: { count: counts["NEEDS-ACTION"]?.length || 0 },
       },
     ]);
-    this.#summary.textContent = new Intl.ListFormat(undefined, {
-      style: "narrow",
-    }).format(translations);
+    this.#summary.textContent = new Intl.ListFormat(
+      Services.locale.appLocalesAsBCP47,
+      {
+        style: "narrow",
+      }
+    ).format(translations);
   }
 }
 
 customElements.define(
   "calendar-dialog-attendees-row",
   CalendarDialogAttendeesRow
 );

