# HG changeset patch
# User Betterbird <betterbird@betterbird.eu>
# Parent  66d73ed82f3d39438b99a887bdf71407033ce387
Misc: Change linting config to allow console.log(), etc.

diff --git a/eslint.config.mjs b/eslint.config.mjs
--- a/eslint.config.mjs
+++ b/eslint.config.mjs
@@ -472,8 +472,17 @@ export default [
    * - Enable curly.
    * - Rollouts
    */
 
+  // Allow console.log() in Betterbird's system modules.
+  {
+    name: "comm-allow-console-log-in-sysmjs",
+    files: ["**/*.sys.mjs"],
+    rules: {
+      "no-console": ["error", { allow: ["createInstance", "error", "info", "log", "warn"] }],
+    },
+  },
+
   // Turn off rules that conflict with Prettier.
   { name: "eslint-config-prettier", ...eslintConfigPrettier },
   {
     name: "enable-curly",
