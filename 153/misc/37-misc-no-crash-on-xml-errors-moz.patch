# HG changeset patch
# User Betterbird <betterbird@betterbird.eu>
# Parent  dc3d82089d0297743a5552018fba51b6715f2932
Misc: Turn 'missing CSP' crash into a warning since it gets triggered by XML errors already.

diff --git a/dom/security/nsContentSecurityUtils.cpp b/dom/security/nsContentSecurityUtils.cpp
--- a/dom/security/nsContentSecurityUtils.cpp
+++ b/dom/security/nsContentSecurityUtils.cpp
@@ -2007,9 +2007,9 @@ void nsContentSecurityUtils::AssertChrom
       !StaticPrefs::security_browser_xhtml_csp_enabled()) {
     return;
   }
 
-  MOZ_CRASH_UNSAFE_PRINTF("Document (%s) does not have a CSP!", spec.get());
+  NS_WARNING(nsPrintfCString("Document (%s) does not have a CSP! (or parsing error)", spec.get()).get());
 }
 
 #  undef CHECK_DIR
 
